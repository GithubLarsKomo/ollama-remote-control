# RC scenario runner slice

This slice implements issue #130 as a foundation step toward #123. It adds the deterministic bounded scenario runner and its evidence contract/tests only. The release-candidate acceptance issue #123 remains open until a subsequent slice executes this runner inside the disposable SSH/Docker/Ollama fixture and uploads the resulting exact-SHA evidence.
